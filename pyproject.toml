[project]
name = "local-code-agent"
version = "0.1.0"
description = "基于大语言模型的本地代码助手"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
